# Strapi Content Types

This document describes the current Strapi content-model state for the baseplate.

## Current State

- No project-specific content types are carried forward in source.
- No seeded schema remains in the repo.
- The backend is now a clean Strapi base ready for new modeling work.

## Rebuild Rules

- Create only the content types `[project-name]` actually needs.
- Prefer small, explicit schemas over speculative modeling.
- Update this document after the first real content model is defined.
