# [project-name] HiFi Site Structure Plan

This document describes the current visual starter state of the frontend and how to adapt it for `[project-name]`.

## Current State

- The frontend currently ships a minimal placeholder shell in `frontend/src/app/`.
- The current implementation is intended to be a reusable baseplate, not a brand-owned reference.
- Replace placeholder copy, metadata, and navigation with real project content as implementation progresses.

## Token Surface

| Token Surface | Starter Value | Usage |
|---------------|---------------|-------|
| `brand-primary` | `#F56024` | Primary CTA / emphasis color |
| `brand-primary-dark` | `#BF3700` | Hover / pressed state |
| `brand-ink` | `#151515` | Primary text, dark backgrounds |
| `surface-base` | `#FFFFFF` | Light surfaces and inverse text |
| `surface-muted` | `#F9F9F9` | Light section backgrounds |
| `text-muted` | `#808080` | Secondary copy |
| `accent` | `#53C507` | Support accent color |
| `accent-soft` | `#C8FFA3` | Soft accent fill |
| `surface-tint` | `#F4FCEE` | Tinted background surface |

## Implementation Notes

- Treat the current visuals and copy as placeholders only.
- Replace the shell, metadata, CTA labels, and navigation before production launch.
- Introduce new page structures only after the real information architecture for `[project-name]` is confirmed.
