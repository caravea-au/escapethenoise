{
  "kind": "collectionType",
  "collectionName": "dealer_submissions",
  "info": {
    "singularName": "dealer-submission",
    "pluralName": "dealer-submissions",
    "displayName": "Dealer Submission",
    "description": "Dealership onboarding form submissions from /dealer-directory-onboarding. Map coordinates live here, on the dealer, so staff see the pin beside the address and lat/long can be exported to the CRM without a join. They previously sat in a separate dealer-geocode collection, kept apart because the client had forbidden adding fields here; that constraint was lifted 2026-08-11. latitude/longitude/precision are PUBLIC (published on /find-dealer via PUBLIC_DEALER_FIELDS); geocodeSource/matchedAddress/geocodedAddress are private and must stay that way — matchedAddress is raw upstream Nominatim text. All six are SERVER-OWNED: create() strips them from client input and writes only what validateDealerPin returns, because this collection's create is a public unauthenticated route."
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "dealershipName": { "type": "string", "required": true },
    "legalName": { "type": "string", "required": true },
    "abn": { "type": "string", "required": true },
    "established": { "type": "integer" },
    "dms": { "type": "string" },
    "dmsOther": { "type": "string" },

    "street": { "type": "string", "required": true },
    "suburb": { "type": "string", "required": true },
    "state": {
      "type": "enumeration",
      "enum": ["VIC", "NSW", "QLD", "SA", "WA", "TAS", "NT", "ACT"]
    },
    "postcode": { "type": "string", "required": true },

    "latitude": { "type": "float" },
    "longitude": { "type": "float" },
    "precision": { "type": "enumeration", "enum": ["street", "approx"] },
    "geocodeSource": {
      "type": "enumeration",
      "enum": ["geocoded", "adjusted", "imported", "admin"],
      "private": true
    },
    "matchedAddress": { "type": "text", "private": true },
    "geocodedAddress": { "type": "text", "private": true },

    "motorDealerLicenceName": { "type": "string" },
    "motorDealerLicenceNumber": { "type": "string" },
    "multipleLocations": { "type": "boolean", "default": false },

    "phone": { "type": "string", "required": true },
    "leadsEmail": { "type": "email", "required": true },
    "smsNumber": { "type": "string" },
    "contactName": { "type": "string", "required": true },
    "contactRole": { "type": "string" },
    "enquiriesEmail": { "type": "email" },

    "tradingHours": { "type": "json" },

    "services": { "type": "json" },
    "servicesOther": { "type": "string" },
    "brands": { "type": "json" },
    "brandsOther": { "type": "string" },
    "productTypes": { "type": "json" },
    "productsOther": { "type": "string" },
    "stockCondition": {
      "type": "enumeration",
      "enum": ["New", "Used", "Both"],
      "default": "Both"
    },
    "financeAvailable": { "type": "boolean", "default": false },
    "deliveryAvailable": { "type": "boolean", "default": false },
    "rvmapBadged": { "type": "boolean", "default": false },
    "rvmasterBadged": { "type": "boolean", "default": false },

    "website": { "type": "string" },
    "description": { "type": "text", "maxLength": 320 },
    "logo": { "type": "string" },
    "photos": { "type": "json" },
    "mediaErrors": { "type": "json" },
    "facebook": { "type": "string" },
    "instagram": { "type": "string" },
    "youtube": { "type": "string" },
    "googleProfile": { "type": "string" },

    "submitterName": { "type": "string", "required": true },
    "submitterEmail": { "type": "email", "required": true },
    "submitterPhone": { "type": "string" },
    "stateAssociation": { "type": "string" },
    "authorised": { "type": "boolean", "default": false },
    "privacyConsent": { "type": "boolean", "default": false },
    "marketingConsent": { "type": "boolean", "default": false },

    "submittedAt": { "type": "datetime" },
    "spamSuspect": { "type": "boolean", "default": false }
  }
}
